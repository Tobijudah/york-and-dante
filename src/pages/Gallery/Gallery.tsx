import React from "react";
import Nav from "../../components/Nav/Nav";
import { PageProps } from "../page.types";

const Gallery: React.FC<PageProps> = ({ navOnClick, preloaded }) => {
	return (
		<div>
			buhgbhkuvhj
			<Nav onClick={navOnClick} />
		</div>
	);
};

export default Gallery;
